package business;

public class Format {
    public enum GameFormat {
        PHYSICAL, DIGITAL
    }

    public enum MovieFormat {
        STANDARD, HD, UHD // free, sliver, gold?
    }

    public enum TvFormat {
        STANDARD, HD, UHD // free, sliver, gold?
    }
}
